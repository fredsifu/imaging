<div class="row">
  <div class="well col-sm-8 col-sm-offset-2">
    <div class="col-sm-11">
      <div class="phone_category"><%= @phone_category.name %></div>
      <div><%= @phone.number %></div>
    </div>
    <div class="col-sm-1">
      <%= link_to edit_phone_path(@phone), class: "btn btn-warning" do %>
        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      <% end %>
    </div>
  </div>
</div>